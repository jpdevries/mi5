{% if options.length %}
  {% set list = 'quantity_list' %}
{% endif %}


{% if not allowBlank|default(true) %}
  {% set required = false %}
{% else %}
  {% set required = true %}
{% endif %}

<label for="{{id|default("number")}}">Quantity</label>
{% include './../twag/input.twig' with {
  type:"number",
  id:id|default("number"),
  name:name|default("number"),
  min:min,
  max:max
} %}

{% if list %}
  <datalist id="{{list}}">
  {% for option in options %}
    <option>{{option.title}}</option>
  {% endfor %}
  </datalist>
{% endif %}
